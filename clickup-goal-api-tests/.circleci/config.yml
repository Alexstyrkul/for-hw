version: 2.1

executors:
  node-executor:
    docker:
      - image: cimg/node:20.5.1

jobs:
  test-cypress:
    executor: node-executor
    working_directory: ~/project
    environment:
      CLICKUP_TOKEN: pk_200540127_JVDGGDGR64FUSZZJDAI0UV1C2WJ9GETA
      LIST_ID: 901511749764
      WORKSPACE_ID: 90151244709
      GOAL_FOLDER_ID: 90157352427
    steps:
      - checkout:
          path: ~/project
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Run Cypress tests
          command: npx cypress run --e2e

workflows:
  version: 2
  run-cypress:
    jobs:
      - test-cypress
